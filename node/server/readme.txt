because I don't create an package.json
so you can ethier use global package, 
installing method: sudo npm install -g [package name]
and generating symbolic link by: npm link [package name]
or 
installing locally: npm install [package name]
